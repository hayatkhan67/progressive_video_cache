name: progressive_video_cache
description: Progressive file-based video caching for Flutter Reels apps. Play from growing local files with instant offline playback.
version: 1.0.0
homepage: https://github.com/hayatkhan67/progressive_video_cache
repository: https://github.com/hayatkhan67/progressive_video_cache
issue_tracker: https://github.com/hayatkhan67/progressive_video_cache/issues

topics:
  - video
  - cache
  - reels
  - offline
  - progressive

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ">=3.0.6 <4.0.0"
  path_provider: ">=2.1.5 <3.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: